# pnpm workspace settings.
# Hoist Vite/React/Tailwind types so workspace packages can import them
# without per-package devDependency duplication. Restrict the rest of node_modules.
shamefully-hoist=false
strict-peer-dependencies=false
auto-install-peers=true
