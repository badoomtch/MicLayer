# scripts/

Developer and CI helpers.

**Status:** SCAFFOLD — populated as milestones land.

Planned:

- `scripts/dev.ps1` — start Tauri dev + watch shared package.
- `scripts/release.ps1` — version bump + tag.
- `scripts/sign-driver.ps1` — submit driver to Microsoft Partner Center for attestation signing (v1.0+).
- `scripts/check-third-party.ps1` — regenerate `THIRD_PARTY.md` from Cargo.lock + pnpm-lock.yaml.
